- flash.each do |name, msg|
  - if msg.is_a?(String)
    .alert.alert-dismissible.fade.in{:role => "alert", :class => "alert-#{name.to_s == 'notice' ? 'success' : 'danger'}"}
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"} ×
      = msg
